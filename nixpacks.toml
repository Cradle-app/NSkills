# Nixpacks configuration for Railway deployment
# Specify Node.js version that meets Prisma requirements (20.19+, 22.12+, 24.0+)

[phases.setup]
nixPkgs = ["nodejs_22", "pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm build"]
