# Nixpacks configuration for Railway
# Prisma requires Node.js 20.19+, 22.12+, or 24.0+

# Use Node 20 LTS which has better version support in nixpkgs
[phases.setup]
nixPkgs = ["nodejs_20", "pnpm-9_x", "openssl"]

# Override to ensure we get a compatible version
[variables]
NODE_ENV = "production"
